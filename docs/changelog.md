# Changelog

## v0.1.0

Initial release.

- On-demand Claude Code execution on Fly.io machines
- Three execution modes: fire-and-forget, streaming, concurrent batch
- Guaranteed machine cleanup via try/finally
- Real-time log streaming via HTTP log drains
- Docker image with Claude Code, git, and gh CLI pre-installed
